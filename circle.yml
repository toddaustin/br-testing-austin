test:
  override:
    - ./node_modules/.bin/grunt ci

#is this working????
# deployment:
#  staging:
#    branch: master
#    commands:
#      - node_modules/.bin/divshot deploy staging --token $DIVSHOT_TOKEN
